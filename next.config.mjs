/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["images.unsplash.com"],
    domains: ["images.photowall.com"], // Add multiple domains here
  },
};

export default nextConfig;
